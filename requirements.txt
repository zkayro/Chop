# Viral clip pipeline with optional OpenAI candidate reranking
streamlit
yt-dlp
faster-whisper
numpy
opencv-python>=4.8,<5
openai>=1.66.0
# Captions are burnt in via ffmpeg's ASS engine (no extra package needed).
